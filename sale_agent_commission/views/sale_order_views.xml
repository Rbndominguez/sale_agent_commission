<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_commission" model="ir.ui.view">
        <field name="name">sale.order.form.commission</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="agent_id"/>
                <field name="commission_rate" invisible="not agent_id"/>
                <field name="commission_amount" invisible="not agent_id"/>
                <field name="commission_amount_margin" invisible="not agent_id"/>
            </field>
        </field>
    </record>
    <record id="view_order_list_commission" model="ir.ui.view">
    <field name="name">sale.order.list.commission</field>
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.sale_order_list_upload"/>
    <field name="arch" type="xml">
        <field name="user_id" position="after">
            <field name="commission_amount" optional="hide"/>
        </field>
    </field>
</record>
</odoo>